// The app config class is used to store application config variables(liek the api endpoint url) in a single place that is easily injected into any component
export class AppConfig{
    public readonly apiUrl='http://localhost:3000';
}